import React, { Component } from 'React'

let quizData = require('./quiz_data.json')

class Quiz extends Component {
    render() {
        return (
            <div />
        )
    }
}

export default Quiz